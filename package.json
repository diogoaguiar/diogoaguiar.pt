{
    "private": true,
    "scripts": {
        "build": "npx @tailwindcss/cli -i src/app.css -o dist/app.css --minify",
        "dev": "npx @tailwindcss/cli -i src/app.css -o dist/app.css --watch"
    },
    "devDependencies": {
        "tailwindcss": "^4.0",
        "@tailwindcss/cli": "^4.0",
        "@tailwindcss/typography": "^0.5"
    }
}
